#pragma once
#include "base/empty-dospq.h"
#include "base/full-dosqp.h"

#define REGISTER_CONTAINS_BENCHMARK(DOSPQ_TYPE, KEY_TYPE) \
    REGISTER_FULL_DOSPQ_BENCHMARK(DOSPQ_TYPE, KEY_TYPE, contains);

#define REGISTER_PREDECESSOR_BENCHMARK(DOSPQ_TYPE, KEY_TYPE) \
    REGISTER_FULL_DOSPQ_BENCHMARK(DOSPQ_TYPE, KEY_TYPE, predecessor);

#define REGISTER_SUCCESSOR_BENCHMARK(DOSPQ_TYPE, KEY_TYPE) \
    REGISTER_FULL_DOSPQ_BENCHMARK(DOSPQ_TYPE, KEY_TYPE, successor);

#define REGISTER_MIN_BENCHMARK(DOSPQ_TYPE, KEY_TYPE) \
    REGISTER_FULL_DOSPQ_BENCHMARK(DOSPQ_TYPE, KEY_TYPE, min);

#define REGISTER_MAX_BENCHMARK(DOSPQ_TYPE, KEY_TYPE) \
    REGISTER_FULL_DOSPQ_BENCHMARK(DOSPQ_TYPE, KEY_TYPE, max);

#define REGISTER_INSERT_BENCHMARK(DOSPQ_TYPE, KEY_TYPE) \
    REGISTER_EMPTY_DOSPQ_BENCHMARK(DOSPQ_TYPE, KEY_TYPE, insert);

#define REGISTER_REMOVE_BENCHMARK(DOSPQ_TYPE, KEY_TYPE) \
    REGISTER_EMPTY_DOSPQ_BENCHMARK(DOSPQ_TYPE, KEY_TYPE, remove);

#define REGISTER_BENCHMARKS_HELPER(REGISTER_MACRO, DOSPQ_TYPE) \
    REGISTER_MACRO(DOSPQ_TYPE, uint64_t) \
    REGISTER_MACRO(DOSPQ_TYPE, uint32_t) \
    REGISTER_MACRO(DOSPQ_TYPE, uint16_t) \
    REGISTER_MACRO(DOSPQ_TYPE, uint8_t)

#define REGISTER_CONTAINS_BENCHMARKS(DOSPQ_TYPE) \
    REGISTER_BENCHMARKS_HELPER(REGISTER_CONTAINS_BENCHMARK, DOSPQ_TYPE)

#define REGISTER_PREDECESSOR_BENCHMARKS(DOSPQ_TYPE) \
    REGISTER_BENCHMARKS_HELPER(REGISTER_PREDECESSOR_BENCHMARK, DOSPQ_TYPE)

#define REGISTER_SUCCESSOR_BENCHMARKS(DOSPQ_TYPE) \
    REGISTER_BENCHMARKS_HELPER(REGISTER_SUCCESSOR_BENCHMARK, DOSPQ_TYPE)

#define REGISTER_MIN_BENCHMARKS(DOSPQ_TYPE) \
    REGISTER_BENCHMARKS_HELPER(REGISTER_MIN_BENCHMARK, DOSPQ_TYPE)

#define REGISTER_MAX_BENCHMARKS(DOSPQ_TYPE) \
    REGISTER_BENCHMARKS_HELPER(REGISTER_MAX_BENCHMARK, DOSPQ_TYPE)

#define REGISTER_INSERT_BENCHMARKS(DOSPQ_TYPE) \
    REGISTER_BENCHMARKS_HELPER(REGISTER_INSERT_BENCHMARK, DOSPQ_TYPE)

#define REGISTER_REMOVE_BENCHMARKS(DOSPQ_TYPE) \
    REGISTER_BENCHMARKS_HELPER(REGISTER_REMOVE_BENCHMARK, DOSPQ_TYPE)
